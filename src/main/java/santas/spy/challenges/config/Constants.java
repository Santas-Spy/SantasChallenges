package santas.spy.challenges.config;

public class Constants {
    /**
     * This will eventually all be loaded from a config file
     * But for now its just hard coded for testing
     * */
    public static String WORLD_NAME = "ChallengesWorld";
    public static int MAX_ISLANDS = 20;
    public static int DIST_BETWEEN_ISLANDS = 100;
}
